const Encabezado4 = ({ clases, titulo }) => {
    return (
        <div>
            <h4 className={`${clases} text-3xl font-semibold`}>{titulo}</h4>
        </div>
    )
}

export default Encabezado4